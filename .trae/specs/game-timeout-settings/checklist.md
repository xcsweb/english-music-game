- [x] 确保 `useSettingsStore` 已被创建并且在应用中被挂载（可以持久化存储到 LocalStorage）。
- [x] 创建了 `SettingsModal.vue` 并包含了“超时秒数”、“自动提示填入”和“自动进入下一句”的开关设置项。
- [x] 在 `Home.vue` 的右上角添加了“设置（Settings）”齿轮图标，点击可打开弹窗。
- [x] 在 `Game.vue` 的右上角添加了“设置（Settings）”齿轮图标，点击可打开弹窗。
- [x] 当进入“拼装”阶段时，倒计时正常启动并在界面上显示（默认 30 秒）。
- [x] 当“拼装”倒计时归零时，系统停止等待用户输入，进入超时处理流程。
- [x] 若开启了“自动提示填入”，超时时单词会自动按照正确的顺序排入句子，并播放该句子的音频。
- [x] 音频播放结束后，如果开启了“自动进入下一句”，游戏自动切换到下一句的“记忆”阶段。
- [x] 音频播放结束后，如果关闭了“自动进入下一句”，游戏会将当前填入的单词清空并重置为散乱状态，倒计时重新开始，要求用户自己填对为止。
- [x] 所有修改后的单元测试与 E2E 自动化测试均已 100% 通过，没有任何阻塞或报错。