- [x] 点击任意按钮（特别是游戏内的拼装操作、返回按钮等）不会引发页面整体刷新（无加载圈、无白屏闪烁）。
- [x] 浏览器的前进/后退操作正常，页面切换通过 Vue Router 的 SPA 模式平滑渲染。
- [x] 脚本成功调用 iTunes API，向 `public/audio` 目录下载了 100 首试听音乐（MP3/M4A 格式）。
- [x] 系统内置的 100 首歌曲能够被读取，且每一句歌词都有通过自动启发式对齐算法生成的 `startTime` 和 `endTime`。
- [x] 旧的测试文件已被彻底清理（`tests/game.spec.ts`, `src/stores/*.spec.ts`, `src/utils/sound.spec.ts`）。
- [x] 针对 100 首歌新规模编写的 E2E 测试运行通过，涵盖游戏载入、音频加载、正确点选歌词及成功跳转等关键业务流程。